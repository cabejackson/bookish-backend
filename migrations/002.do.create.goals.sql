
CREATE TABLE goals (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  tbr_number INTEGER NOT NULL,
  timeframe TEXT NOT NULL,
  reading_goals TEXT NOT NULL,
  date_created TIMESTAMPTZ DEFAULT now() NOT NULL
);